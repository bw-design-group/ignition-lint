{
  "_comment": "Configuration with rules enabled by default. Severity levels are defined by rule developers - users don't configure severity.",
  "PylintScriptRule": {
    "enabled": true,
    "kwargs": {
      "_comment": "Optional: Specify a custom pylintrc file path. If not specified, uses .config/ignition.pylintrc",
      "pylintrc": null
    }
  },
  "PollingIntervalRule": {
    "enabled": true,
    "kwargs": {
      "minimum_interval": 10000
    }
  },
  "NamePatternRule": {
    "enabled": true,
    "kwargs": {
      "target_node_types": [
        "component",
        "property"
      ],
      "node_type_specific_rules": {
        "component": {
          "convention": "PascalCase",
          "min_length": 3
        },
        "property": {
          "convention": "camelCase",
          "min_length": 2
        }
      }
    }
  },
  "UnusedCustomPropertiesRule": {
    "enabled": true,
    "kwargs": {}
  },
  "_info": {
    "_description": "Rule severity levels are defined by rule developers:",
    "rule_severities": {
      "NamePatternRule": "warning - naming conventions are style preferences",
      "PollingIntervalRule": "error - performance issues that can cause system problems",
      "PylintScriptRule": "error - syntax errors, undefined variables, etc."
    },
    "note": "Users cannot configure severity levels - they are determined by what each rule checks.",
    "PylintScriptRule_config": {
      "pylintrc": "Path to custom pylintrc file. Defaults to .config/ignition.pylintrc if not specified.",
      "example": "\"pylintrc\": \"/path/to/custom.pylintrc\" or \"pylintrc\": \"relative/path/.pylintrc\""
    }
  }
}
